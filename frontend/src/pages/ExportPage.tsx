import PlaceholderScreen from '../components/PlaceholderScreen'

export default function ExportPage() {
  return (
    <PlaceholderScreen
      phase="Phase E"
      title="Export"
      description="Executive Excel export with exactly-one-batch-per-platform validation will live here."
    />
  )
}
